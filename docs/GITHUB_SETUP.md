# GitHub setup

The local repository is on `main`. After local validation, run `gh auth status`, then `gh repo create cribbit-chaos --private --source . --remote origin` if the remote does not already exist. Inspect `git status` and `git remote -v` before the first push.
