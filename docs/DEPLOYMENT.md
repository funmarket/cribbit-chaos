# Deployment

Deploy `apps/web` and `apps/telegram` as independent Vercel projects from this repository. Deploy `apps/api` to Railway with a Railway PostgreSQL service. Deployment-specific instructions are in `VERCEL_SETUP.md` and `RAILWAY_SETUP.md`.
