# Vercel setup

Create two projects from the same GitHub repository. Set Root Directory to `apps/web` or `apps/telegram`. The checked-in Vercel config uses the root workspace build command and `dist` output. Set only `VITE_API_URL`, `VITE_REALTIME_URL`, and `VITE_APP_ENV` in each project.
