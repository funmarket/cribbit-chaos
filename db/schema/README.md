# PostgreSQL schema

`../migrations/001_initial.sql` is the current foundation for internal UUID users, provider identities, rooms, sessions, commands, events, prompts, answers, saved prompts, and recaps. Apply migrations through the Railway PostgreSQL connection before enabling production API authentication.
